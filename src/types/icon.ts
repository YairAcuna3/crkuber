export type Icon = {
  size?: number;
  lightColor?: string;
  darkColor?: string;
  className?: string;
  href?: string;
  target?: string;
  isDark?: boolean;
};

export type IconOption = {
  Icon: any;
  url: string;
};
